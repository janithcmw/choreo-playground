// Public URL of the deployed greeting-service in Choreo
configurable string greetingServiceUrl = ?;

// Asgardeo OAuth2 credentials — store clientSecret as a Choreo Secret, not plain config
configurable string asgardeoTokenUrl = ?;
configurable string clientId = ?;
configurable string clientSecret = ?;
